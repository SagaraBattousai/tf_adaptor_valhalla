#ifndef __VALHALLA_FEDERATED_MODEL_H__
#define __VALHALLA_FEDERATED_MODEL_H__
/*
#include <_falcie_config.h>

#include <memory>

#include <valhalla/model.h>

namespace valhalla {

//class FALCIE_EXPORT FederatedModel : public Model {
  //inline so remove export for now
class FederatedModel : public Model {
 public:
  FederatedModel(const char *filename) : Model(filename) {}
  FederatedModel(const char *filename, int num_threads)
      : Model(filename, num_threads) {}

  // tflite::SignatureRunner *GetSignatureRunner();

 //private:

};

}  // namespace valhalla
*/
#endif